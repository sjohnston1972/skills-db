node_modules
npm-debug.log
.git
.gitignore
README.md
.DS_Store
*.md
# Local secrets must never enter the build context copies (.htpasswd is the
# one deliberate exception — nginx needs it and the Dockerfile COPYs it).
.env
.env.*
!.env.example
creds.txt
